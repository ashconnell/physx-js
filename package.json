{
  "name": "physx-js",
  "version": "0.0.1",
  "author": "Ash Connell <hello@ashconnell.com>",
  "main": "dist/physx.release.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ashconnell/physx-js.git"
  },
  "bugs": {
    "url": "https://github.com/ashconnell/physx-js/issues"
  },
  "devDependencies": {
    "physx": "prestomation/PhysX#emscripten_wip"
  },
  "scripts": {
    "test": "docker run --rm -it -v $(pwd):/src trzeci/emscripten /bin/bash",
    "build": "docker run --rm -v $(pwd):/src trzeci/emscripten ./build.sh"
  },
  "engines": {
    "node": ">=10.16.0"
  }
}
